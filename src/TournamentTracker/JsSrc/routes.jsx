import { Route } from 'react-router'; //Use # history instead of HTML5 history API

import App from './components/App';
import {HomePageContainer} from './components/HomePage';

export default <Route path="/" component ={App}>
    
</Route>;